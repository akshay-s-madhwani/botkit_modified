/**
 * Clean any given test
 *
 * @param {*} text string
 */
exports.cleanText = function cleanText(text) {
  // clean it and return
  return text;
};

/**
 * Check if a funtion is inside a certain range
 *
 * @export
 * @param {*} text
 * @param {*} min
 * @param {*} max
 */
exports.isWithinRange = function isWithinRange(text, min, max) {
  // check if text is between min and max length
  return { text, min, max };
};
